{
  "name": "grimoirejs-build-env-base",
  "version": "1.0.3",
  "description": "The common files to build grimoirejs or bundling.",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel index.js --out-file lib/index.js --presets es2015,stage-2 --plugin transform-runtime",
    "prepublish":"npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^1.1.3",
    "fs-extra": "^0.30.0",
    "glob": "^7.0.5",
    "handlebars": "^4.0.5",
    "watch": "^0.19.1"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-plugin-transform-runtime": "^6.12.0"
  }
}
